object abstract Super {

    pure value() => self.value

data:
    value: integer

factory:
    pure new(value: integer) => { value: value }
}

object Sub: Super {

    pure subValue() => self.subValue

data:
    subValue: integer

factory:
    pure init(subValue: integer) => {
        // super.new(value: superValue)
        subValue: subValue
    }
}

print 2
// let x: Sub.new(superValue: 13, subValue: 11)
// print x.superValue() - x.subValue()
